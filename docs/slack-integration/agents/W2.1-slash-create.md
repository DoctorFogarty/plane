# W2.1 — Slash /plane create modal

Read first: [../CONTEXT.md](../CONTEXT.md)

## Depends on

D1.

## Parallel with

W5 after D1.

## Goal

/plane (ignore extra text in MVP) → project picker (member projects only) → work-item modal → create as mapped user → post issue card to channel.

## In scope

- Connect-gate ephemeral if unmapped.
- trigger_id used before Plane round-trips.
- Tests with signed slash/interactivity fixtures.
- Deploy API.

## Out of scope

Intake. Required custom properties beyond title/description/priority/state/assignees/labels if extra APIs needed.

## Read / touch

W0.2 block-kit-inventory.md; Silo handle-command.ts / issue-modal-full.ts as spec (git 9b641e6407).

## Skills

validate-and-deploy-vps (API).

## Done when

Unauthenticated Slack user gets connect-gate; trigger_id used before Plane calls; tests pass; VPS deploy so /plane hits live workers.

## Prompt

Implement /plane slash command that opens a create-work-item modal and posts a result card. Port Silo modal fields. Require mapped user. Validate and deploy API to the VPS.

## Handoff (fill when complete)

- Files changed:
- APIs added:
- Tests:
- Deployed? (URL / smoke):
- Risks for the next agent:
