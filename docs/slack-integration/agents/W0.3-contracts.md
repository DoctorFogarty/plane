# W0.3 — Contracts: data model, APIs, threat model, deploy modes

Read first: [../CONTEXT.md](../CONTEXT.md)

## Depends on

W0.1 (and W0.2 if ready).

## Parallel with

None after W0.1.

## Goal

Lock Django models, HTTP routes, instance config keys, and security rules so Wave 1 does not invent schema.

## In scope

- Write `docs/slack-integration/contracts.md` covering SlackWorkspaceConnection, SlackUserConnection, SlackChannelSubscription, SlackThreadLink, preference channel key, instance keys, ingress routes, OAuth callbacks, threat table.
- Lock VPS callback paths under https://plane.teamcodeorange.com.
- Mark SlackProjectSync deprecated.

## Out of scope

Application code. Socket Mode on this VPS (deferred to W8.1 for other hosts).

## Read / touch

CONTEXT.md; W0.1 capability-matrix.md; W0.2 catalogs if present; existing GitHub integration models/views.

## Skills

None (spec only).

## Done when

Field lists and unique constraints explicit; SlackProjectSync deprecated; HTTP vs Socket Mode documented; VPS callback paths written down.

## Prompt

From CONTEXT.md, write implementation contracts for models, APIs, instance config, threat mitigations, and VPS public URLs at plane.teamcodeorange.com. Do not implement. Do not reuse SlackProjectSync.

## Handoff (fill when complete)

- Files changed:
- APIs added:
- Tests:
- Deployed? (URL / smoke):
- Risks for the next agent:
