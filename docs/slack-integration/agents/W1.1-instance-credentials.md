# W1.1 — Instance Slack credentials

Read first: [../CONTEXT.md](../CONTEXT.md)

## Depends on

W0.3 contracts.md.

## Parallel with

None.

## Goal

God Mode / instance config stores client id, secret, and signing secret; expose only client id to the frontend.

## In scope

- Add SLACK_CLIENT_SECRET and signing secret following the GitHub PEM/secret pattern.
- Keep SLACK_CLIENT_ID on public GET /api/instances/.
- Tests for presence/absence gating.

## Out of scope

OAuth, views beyond instance config, deploy.

## Read / touch

`apps/api/plane/license/api/views/instance.py`; `packages/types/src/instance/base.ts`.

## Skills

None of the web/deploy trio required. Validate with pytest only.

## Done when

Secret is not in GET /api/instances/ public config; tests cover presence/absence gating.

## Prompt

Add instance configuration for Slack client secret and signing secret following the GitHub PEM/secret pattern. Do not implement OAuth yet.

## Handoff (fill when complete)

- Files changed:
- APIs added:
- Tests:
- Deployed? (URL / smoke):
- Risks for the next agent:
