# W1.5 — Dual OAuth (workspace + user)

Read first: [../CONTEXT.md](../CONTEXT.md)

## Depends on

W1.4.

## Parallel with

None.

## Goal

Workspace bot install + per-user link; one Slack team ↔ one Plane workspace; store encrypted tokens; app_uninstalled / revoke clears rows.

## In scope

- OAuth callbacks under /api/hooks/slack/oauth/.
- Allow slack on WorkspaceIntegrationEndpoint (GitHub-only today).
- Bot user + service APIToken like GitHub.
- Reject duplicate Slack team installs.
- No email-only auto-link.
- Deploy API image.

## Out of scope

Create-from-Slack. Settings UI (W1.6).

## Read / touch

`apps/api/plane/app/views/integration/base.py`; new OAuth views; W1.2 models.

## Skills

validate-and-deploy-vps (API).

## Done when

Contract tests for install, duplicate-team rejection, user link, uninstall; VPS deploy so Slack OAuth redirect hits this instance.

## Prompt

Implement Slack workspace and user OAuth against the new connection models. Follow GitHub WorkspaceIntegration for the bot actor. Reject duplicate Slack team installs. Validate and deploy the API image to the VPS.

## Handoff (fill when complete)

- Files changed:
- APIs added:
- Tests:
- Deployed? (URL / smoke):
- Risks for the next agent:
