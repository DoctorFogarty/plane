# W1.3 — Slack client, signing, token refresh lock

Read first: [../CONTEXT.md](../CONTEXT.md)

## Depends on

W1.2.

## Parallel with

None.

## Goal

Shared helper: verify Slack signatures, chat.postMessage / views.open / chat.update, token refresh with single-flight lock, never log tokens.

## In scope

- New module under `apps/api/plane/utils/slack/`.
- Use pinned slack-sdk.
- Unit tests for signature verify, expired-token refresh race, and redacted logging.

## Out of scope

HTTP views except what tests need. OAuth. Deploy.

## Read / touch

`apps/api/plane/utils/slack/`; W1.2 models for token storage.

## Skills

Validate with pytest only. No deploy.

## Done when

Unit tests pass for signature verify, refresh race, and redacted logging.

## Prompt

Implement a Slack API client with request signing verification and locked token rotation. No HTTP views except what tests need.

## Handoff (fill when complete)

- Files changed:
- APIs added:
- Tests:
- Deployed? (URL / smoke):
- Risks for the next agent:
