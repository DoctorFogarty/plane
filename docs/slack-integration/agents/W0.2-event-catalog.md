# W0.2 — Event catalog + Silo Block Kit extract

Read first: [../CONTEXT.md](../CONTEXT.md)

## Depends on

Nothing.

## Parallel with

W0.1.

## Goal

Map Plane IssueActivity.field values to Slack templates and preference gates; extract Silo Block Kit shapes from git history.

## In scope

- Write `docs/slack-integration/event-catalog.md` (activity field → DM? channel? skip? template).
- Write `docs/slack-integration/block-kit-inventory.md` (issue card, create modal, connect gate).
- Note Block Kit 75-character option limit.

## Out of scope

Application code.

## Read / touch

`apps/api/plane/bgtasks/notification_task.py`; `apps/api/plane/bgtasks/issue_activities_task.py`; git `9b641e6407:apps/silo/src/apps/slack/views/`.

## Skills

None (spec only).

## Done when

Email-class events listed; description/reactions/votes/draft marked skip; Silo issue-linkback and create-modal fields inventoried.

## Prompt

Extract Plane activity→notification mapping and historical Silo Block Kit views from git `9b641e6407`. Produce an event catalog and block-kit inventory. No application code.

## Handoff (fill when complete)

- Files changed:
- APIs added:
- Tests:
- Deployed? (URL / smoke):
- Risks for the next agent:
