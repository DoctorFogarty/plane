# W1.4 — HTTP ingress + Celery ack/queue

Read first: [../CONTEXT.md](../CONTEXT.md)

## Depends on

W1.3.

## Parallel with

None.

## Goal

POST Events / Interactivity / Commands: verify, HTTP 200 in 3s, enqueue Celery, idempotent on Slack event_id.

## In scope

- AllowAny + signature (GitHub webhook pattern).
- url_verification challenge.
- Stub worker that logs event type.
- Then validate-and-deploy-vps for the API image.

## Out of scope

Handling business events (unfurl, create). OAuth.

## Read / touch

New urls/views under `apps/api/plane/app/views/` + bgtasks; `apps/api/plane/app/views/integration/webhook.py` as pattern.

## Skills

`.claude/skills/validate-and-deploy-vps/SKILL.md`

## Done when

url_verification works; unsigned requests 401; duplicate event_id is a no-op; VPS deploy so Slack can reach the Request URL.

## Prompt

Add Slack HTTP ingress that verifies signatures, answers url_verification, acks in 3s, and enqueues idempotent Celery jobs. Stub the worker. No OAuth. Then run validate-and-deploy-vps for the API image.

## Handoff (fill when complete)

- Files changed:
- APIs added:
- Tests:
- Deployed? (URL / smoke):
- Risks for the next agent:
