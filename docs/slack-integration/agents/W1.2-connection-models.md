# W1.2 — Connection models + migrations

Read first: [../CONTEXT.md](../CONTEXT.md)

## Depends on

W1.1.

## Parallel with

None.

## Goal

New tables from contracts.md; leave SlackProjectSync in place unused (no destructive data migration).

## In scope

- SlackWorkspaceConnection, SlackUserConnection, SlackChannelSubscription, SlackThreadLink.
- Unique constraints allow multiple channel subscriptions.
- Encrypted token fields (or documented encryption helper used here).
- Migration + model exports.

## Out of scope

Views, OAuth, writing to SlackProjectSync.

## Read / touch

`apps/api/plane/db/models/integration/`; `apps/api/plane/db/models/__init__.py`; new migration; `docs/slack-integration/contracts.md`.

## Skills

Validate with pytest only. No deploy.

## Done when

Models match contracts; multiple channel subscriptions allowed; SlackProjectSync untouched as system of record.

## Prompt

Implement Slack connection models and migrations from docs/slack-integration/contracts.md. Do not add views. Do not write to SlackProjectSync.

## Handoff (fill when complete)

- Files changed:
- APIs added:
- Tests:
- Deployed? (URL / smoke):
- Risks for the next agent:
